/**
 * 独立维护的版本常量（《技术规格 v1》第 24 章）。
 * 所有版本常量只能从这里导出。
 */

export const pluginVersion = "0.1.0";

/** `.components` 文档顶层版本。 */
export const formatVersion = 1;

/** Component SDK 版本。 */
export const componentSdkVersion = 1;

/** PageRecord Schema 版本。 */
export const pageSchemaVersion = 1;

/** RawPageSnapshot 版本。 */
export const rawSnapshotVersion = 1;

/** Vault 解析器版本。 */
export const parserVersion = "v1.0.0";

/** Query DSL 版本。 */
export const queryDslVersion = 1;

/** IndexedDB Schema 版本。 */
export const dbSchemaVersion = 1;

/** Worker 协议版本。 */
export const workerProtocolVersion = 1;
